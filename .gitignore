node_modules/
tools/.cache/
.DS_Store

# Generated review stations — regenerate with `npm run stack --html` / `npm run sweep --html`.
# Kept on disk (they are opened by double-click and work offline), just not versioned:
# together they were 476K of churn in every diff.
tools/review.html
tools/sweep.html

# Per-machine Claude Code permissions — the ".local." is the convention for
# personal config. .claude/launch.json stays versioned: it is the shared dev
# server definition, not a personal setting.
.claude/settings.local.json
